{
  "name": "@tlsnotary/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build-plugins.js --watch & vite",
    "build": "npm run build:plugins && vite build",
    "build:plugins": "node build-plugins.js",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "typecheck:plugins": "tsc --noEmit -p plugins/tsconfig.json",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "esbuild": "^0.24.2",
    "typescript": "^6.0.2",
    "vite": "^7.3.0"
  }
}
