# Built libraries (regenerate with build.sh)
ios/TlsnMobile.xcframework/
ios/lib/

# Generated Swift bindings (regenerate with build.sh)
ios/tlsn_mobile.swift
ios/include/

# Android native artifacts are committed to the repo so that
# EAS Build can include them (EAS can't run cargo/Rust).
# Regenerate with build-android.sh if updating the Rust code.
# android/src/main/jniLibs/
# android/src/main/java/uniffi/
